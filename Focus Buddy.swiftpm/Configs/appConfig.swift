import SwiftUI

enum Endpoint {
    static let baseURL = "https://www.pisan30.online/mobile"
    
    enum Path {
        static let testApi = "/test"
        static let getItems = "/getItems"
    }
}

